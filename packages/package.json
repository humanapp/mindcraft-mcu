{
  "scripts": {
    "install": "for dir in */; do (cd \"$dir\" && npm install) || exit 1; done",
    "build": "if [ -n \"$CI\" ]; then check=check:only; else check=check; fi; for dir in */; do (cd \"$dir\" && npm run \"$check\" --if-present && npm run build --if-present && npm test --if-present) || exit 1; done"
  }
}
