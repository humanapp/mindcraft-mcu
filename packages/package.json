{
  "scripts": {
    "install": "for dir in */; do (cd \"$dir\" && npm install); done",
    "build": "for dir in */; do (cd \"$dir\" && npm run check --if-present && npm run build --if-present && npm test --if-present); done",
    "release:all:patch": "node ../external/mindcraft-lang/scripts/release-all.js patch",
    "release:all:minor": "node ../external/mindcraft-lang/scripts/release-all.js minor",
    "release:all:major": "node ../external/mindcraft-lang/scripts/release-all.js major"
  }
}
