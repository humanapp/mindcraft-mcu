{
  "name": "mindcraft-mcu",
  "description": "Portable embedded runtime and platform targets for the Mindcraft bytecode VM.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "prepare": "git submodule update --init --recursive",
    "bootstrap": "npm run prepare && npm install && npm run build:deps",
    "install": "npm run install:packages && npm run install:apps",
    "install:packages": "npm --prefix packages install",
    "install:apps": "npm --prefix apps install",
    "build:deps": "npm --prefix apps run build:deps"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.15",
    "@types/node": "^25.0.2",
    "chalk": "^5.6.2",
    "tsx": "^4.21.0"
  }
}
